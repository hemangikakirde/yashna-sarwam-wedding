// RSVP emails are sent here (your inbox only — no Google Sheet required).
// After you set this, submit one test RSVP on the live site and confirm the
// verification email from FormSubmit (one-time step).
window.RSVP_EMAIL = "";
